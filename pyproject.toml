[project]
name = "tetris"
version = "1.0.0"
description = "A Tetris game implementation using Pygame"
requires-python = ">=3.7"
dependencies = [
    "pygame>=2.5.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

